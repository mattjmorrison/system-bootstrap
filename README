This is a bootstrap script to setup a new virtual machine linux (debian/ubuntu) machine,
and/or build server(s) for python projects.

Use this in conjunction with Buildout to get a project up and running.
